% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chunk.R
\name{chunk}
\alias{chunk}
\title{Capture a Code Chunk Without Evaluating It}
\usage{
chunk(expr)
}
\arguments{
\item{expr}{An R expression, typically written as a \code{{}} block.
The expression is captured but not evaluated.}
}
\value{
A language object (call) representing the captured expression.
}
\description{
\code{chunk()} is a helper function that captures an expression
without evaluating it. It is primarily designed for use with
\code{ABM_Series()}, where code blocks (chunks) are stored and
executed later by \code{run_Series()} in a controlled environment.

This allows users to write ordinary R code blocks using \code{{}} syntax,
while ensuring that the code is stored as a language object
(i.e., not evaluated immediately).
}
\details{
The function internally uses \code{substitute()} to prevent evaluation.
For example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{chunk(\{
  x <- 1
  y <- x * 2
\})
}\if{html}{\out{</div>}}

stores the code block as a language object rather than executing it.

The returned object can later be evaluated using \code{eval()}
in a specified environment.
}
\examples{
# Create a chunk without executing it
ch <- chunk({
  x <- 1
  y <- x * 2
})

# Inspect structure
str(ch)

# Evaluate later
env <- new.env()
eval(ch, envir = env)
env$x
env$y

}
\seealso{
\code{\link[=ABM_Series]{ABM_Series()}}
}
