% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_Series.R
\name{run_Series}
\alias{run_Series}
\title{Run an ABM Series}
\usage{
run_Series(
  S,
  series_plan = NULL,
  input = NULL,
  series_seed = NULL,
  simplify_output = FALSE,
  verbose = TRUE,
  parent = parent.frame(),
  on_error = c("continue", "stop"),
  keep = NULL
)
}
\arguments{
\item{S}{An object of class \code{"ABM_Series"}.}

\item{series_plan}{A plan specifying which chunks to execute and in what order.
\itemize{
\item \code{NULL} (default): run all chunks in \code{S$chunks} in their stored order.
\item Character vector: chunk names to run (must exist in \code{names(S$chunks)}).
\item Numeric/integer vector: chunk indices to run (1-based). Every element
must be a whole number (e.g. \code{2}, \code{2.0}); non-integer values
(e.g. \code{2.5}) raise an error rather than being silently truncated.
}}

\item{input}{Optional objects to inject into the execution environment.
These objects overwrite entries in \code{S$default} before execution.
Functions are allowed, and so is \code{NULL} (an entry set to \code{NULL} via
\code{input} remains present in the execution environment with value \code{NULL},
rather than being removed). If unnamed, names are auto-generated by the
internal formatter.}

\item{series_seed}{Random seed for the series run. If \code{NULL}, a seed is
generated automatically (see Details).}

\item{simplify_output}{Logical. If \code{TRUE}, return only the resulting objects
(i.e., a named list created from the execution environment). If \code{FALSE}, return a
list with objects and metadata such as the plan, seed, error log, and elapsed time.}

\item{verbose}{Logical. If \code{TRUE}, print the execution plan and step-by-step
progress messages (including errors).}

\item{parent}{Parent environment for the execution environment \code{V}.
Use \code{parent.frame()} (default) to allow chunks to access objects/functions
from the calling context. For more reproducible runs, consider a more isolated
parent such as \code{baseenv()} or \code{emptyenv()}, and pass required objects explicitly
via \code{default} and \code{input}.}

\item{on_error}{Behavior when an error occurs while executing a chunk.
\itemize{
\item \code{"continue"} (default): store the error in \code{error_log} and continue to the next chunk.
\item \code{"stop"}: store the error in \code{error_log}, then stop executing further
chunks and return normally with whatever was accumulated so far (the
partial \code{values}/\code{error_log}/etc.). This does \strong{not} raise an
uncaught error out of \code{run_Series()} itself -- see Details.
}}

\item{keep}{Optional character vector of names to keep in the returned objects.
If \code{NULL} (default), all objects in the execution environment are returned.
If specified, the returned objects (or \code{out$values}) are subset to these names.
Names not found are ignored with a warning.}
}
\value{
If \code{simplify_output = TRUE}, a named list of objects created in the execution
environment (optionally subset by \code{keep}).

If \code{simplify_output = FALSE}, a list with:
\describe{
\item{values}{A named list of objects created in the execution environment
(optionally subset by \code{keep}).}
\item{series_plan}{Character vector of executed chunk names.}
\item{series_seed}{The seed used for the run.}
\item{error_log}{A list of captured error conditions (if any), keyed by step.}
\item{implementation_took}{Elapsed time as a character string (\code{HH:MM:SS.mmm}).}
}
}
\description{
\code{run_Series()} executes stored code chunks in an \code{\link[=Series]{Series()}} object
in a specified order. Chunks are evaluated sequentially in a dedicated
environment, so objects created in earlier chunks (e.g., \code{x <- 1}) can be
referenced by later chunks (e.g., \code{y <- x * 2}).

Default objects stored in \code{S$default} are first loaded into the execution
environment, then optionally overwritten by \code{input}.
}
\details{
Chunks are evaluated using \code{eval()} in a dedicated environment \code{V} created by
\code{list2env()}. The parent of \code{V} is controlled by the \code{parent} argument.

Errors in individual chunks are captured with \code{tryCatch()} around each
chunk's evaluation, so a failing chunk never aborts the whole
\code{run_Series()} call: with \code{on_error = "continue"} (the default), execution
simply moves on to the next chunk; with \code{on_error = "stop"}, the remaining
chunks are skipped (the \code{for} loop is exited via \code{break}, not by
re-raising the error), and \code{run_Series()} still returns its usual result
-- including whatever was captured in \code{error_log} -- rather than
propagating an uncaught error to the caller.

If \code{series_seed} is \code{NULL}, a seed is drawn via
\code{sample.int(.Machine$integer.max, 1L)} (which relies on R's own
auto-initialized RNG state) and recorded in the return value regardless
of whether it was supplied or auto-generated, mirroring the approach used
by \code{run_Game()}.
}
\examples{
step1 <- Chunk({ x <- 1; y <- 2 })
step2 <- Chunk({ z <- add2(x, y) })

S <- Series(
  step1,
  step2,
  default = list(add2 = function(a, b) a + b)
)

# Run the series
out <- run_Series(S, verbose = FALSE)
out$values$z

# Run only a subset by chunk name
out2 <- run_Series(S, series_plan = "step1", verbose = FALSE)

# Inject objects before running
out3 <- run_Series(S, input = list(x = 10), verbose = FALSE)
out3$values$z

# Stop on the first error -- execution halts after the failing chunk,
# but run_Series() still returns normally with the partial results and
# the captured error in error_log.
step_bad <- Chunk({ stop("boom") })
step_after <- Chunk({ never_runs <- TRUE })
S_err <- Series(step1, step_bad, step_after)
out4 <- run_Series(S_err, on_error = "stop", verbose = FALSE)
names(out4$error_log)
out4$values$never_runs  # NULL: step_after was never executed

# Keep only selected objects in output
out5 <- run_Series(S, keep = c("x", "z"), verbose = FALSE)
names(out5$values)

}
\seealso{
\code{\link[=Series]{Series()}}, \code{\link[=Chunk]{Chunk()}}
}
