% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operators-aa.R
\name{\%aa\%}
\alias{\%aa\%}
\alias{\%aa\%<-}
\title{Agent attribute operations}
\usage{
agents \%aa\% val_name

`\%aa\%`(agents, val_name) <- value
}
\arguments{
\item{agents}{A list of ABM_Agent objects.}

\item{val_name}{A character string. The name of the attribute to access or assign.}

\item{value}{A list of values to assign (for \verb{\%aa\%<-}). Must match the length of \code{agents}.}
}
\value{
\itemize{
\item \verb{\%aa\%} returns a named list of attribute values.
\item \verb{\%aa\%<-} returns the modified list of agents.
}
}
\description{
These operators provide access to and assignment of agent attributes
within a list of ABM_Agent objects.
}
\details{
The \verb{\%aa\%} operator extracts a specified attribute from each agent.
The \verb{\%aa\%<-} operator assigns values to a specified attribute in each agent.
}
\examples{
# Create agents with age attribute
agents <- init_agents(n = 3, attr_df = data.frame(age = 1:3))

# Extract and set attributes using \%aa\%
agents \%aa\% "age"
agents \%aa\% "age" <- list(10, 20, 30)

# Use within an ABM_G object (G)
G <- setABM(agents = init_agents(n = 3, attr_df = data.frame(age = 1:3)))
G$agents \%aa\% "age"
G$agents \%aa\% "age" <- list(10, 20, 30)

# Combine with \%ai\% to access subsets
G <- setABM(agents = init_agents(n = 3, attr_df = data.frame(age = 1:3)))
G$agents \%ai\% c(1, 3) \%aa\% "age"
G$agents \%ai\% c(1, 3) \%aa\% "age" <- list(10, 30)

}
