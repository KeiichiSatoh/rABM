% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active_ca_map.R
\name{active_ca_map}
\alias{active_ca_map}
\title{Create an active-binding CA map function (factory)}
\usage{
active_ca_map(
  posit_type = c("lin", "rc"),
  nrow,
  ncol,
  nm_posit,
  nm_attr,
  vacant_val = 0,
  safe = FALSE
)
}
\arguments{
\item{posit_type}{Character. Position format used in \code{self[[nm_posit]]}.
One of \code{c("lin", "rc")}.}

\item{nrow}{Integer (>= 1). Number of rows in the grid.}

\item{ncol}{Integer (>= 1). Number of columns in the grid.}

\item{nm_posit}{Character scalar. Field name in \code{self} that stores positions.}

\item{nm_attr}{Character scalar. Field name in \code{self} that stores attribute values to write.}

\item{vacant_val}{Scalar. Value used to initialize the matrix (default: \code{0}).}

\item{safe}{Logical. If \code{TRUE}, ignore \code{NA} and out-of-bounds positions.
If \code{FALSE}, perform minimal checks for speed (default).}
}
\value{
A function with no required arguments. When evaluated in an \code{ABM_Game} context
(where \code{self} is available), it returns an \code{nrow} x \code{ncol} matrix in which the
entries at specified positions are replaced with the corresponding attribute values.
}
\description{
\code{active_ca_map()} is a factory that returns a \strong{no-argument function} intended to be
registered as an \code{"active_state"} field (i.e., an active binding) in an \code{\link{ABM_Game}}.
When evaluated, the returned function creates a fresh \code{nrow} x \code{ncol} matrix filled with
\code{vacant_val} and then writes agent attributes into the matrix according to the positions
stored in \code{self}.

This function is designed to be compatible with the current \strong{rABM} internal design:
active-binding functions are evaluated with \strong{no arguments}, and their environments may be
overwritten when registered into an \code{ABM_Game}. Therefore, the returned function does not
rely on closure state and builds the matrix on each evaluation.
}
\details{
\subsection{Position specification (\code{posit_type})}{

Users can store positions in either of the following formats:
\itemize{
\item \code{posit_type = "lin"}:
A vector of \strong{linear indices} (1..\code{nrow*ncol}) in \strong{column-major} order (the same as R matrices).
\item \code{posit_type = "rc"}:
A \verb{K x 2} matrix (or data.frame) whose columns are \verb{(row, col)} (1-based).
These are converted internally to linear indices by
\deqn{idx = (col - 1) * nrow + row}
}
}

\subsection{Safety option (\code{safe})}{
\itemize{
\item \code{safe = FALSE} (default): minimal checks for speed.
Out-of-range indices may error (especially for \code{rc}).
\item \code{safe = TRUE}: ignore \code{NA} and out-of-bounds positions (do not assign those entries).
This is useful when user input may contain invalid positions.
}
}
}
\examples{
# Example 1: linear indices (column-major)
city_map <- active_ca_map(
  posit_type = "lin",
  nrow = 5, ncol = 5,
  nm_posit = "posit",
  nm_attr  = "attr",
  vacant_val = 0
)

# In an ABM_Game, 'city_map' is typically registered as active_state.
# Here we only show the expected 'self' structure.
self <- list(posit = c(1, 7, 13), attr = c(10, 20, 30))
# If 'self' were available in the evaluation environment:
# city_map()

# Example 2: (row, col) matrix positions
city_map_rc <- active_ca_map(
  posit_type = "rc",
  nrow = 5, ncol = 5,
  nm_posit = "posit_rc",
  nm_attr  = "attr",
  vacant_val = 0,
  safe = TRUE
)
self <- list(
  posit_rc = matrix(c(1,1,  2,1,  6,2), ncol = 2, byrow = TRUE), # last row is out-of-bounds
  attr     = c(1, 1, 2)
)
# city_map_rc()  # out-of-bounds row would be ignored when safe = TRUE

}
