% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_ABM_Series.R
\name{ABM_Series}
\alias{ABM_Series}
\title{Create an ABM_Series Object}
\usage{
ABM_Series(chunk_expr = NULL, default = NULL)
}
\arguments{
\item{chunk_expr}{A list (or a single object) of chunks to store.
Each element must be a language object (e.g., \code{quote({ ... })},
\code{quote(x <- 1)}, or the output of \code{\link[=chunk]{chunk()}}). If unnamed, names are generated.
If \code{NULL}, an empty chunk list is stored.}

\item{default}{A list (or a single object) of default objects to store.
These objects are injected into the execution environment before running chunks.
Functions are allowed. If unnamed, names are generated. If \code{NULL}, an empty
default list is stored.}
}
\value{
An object of class \code{"ABM_Series"}, a list with the following elements:
\describe{
\item{chunks}{A named list of language objects representing code chunks.}
\item{default}{A named list of default objects (values and/or functions).}
}
}
\description{
\code{ABM_Series()} is a constructor for the \code{"ABM_Series"} class.
It stores a set of code chunks (as language objects) and default objects
(values and/or functions) that will be used to initialize the execution
environment in \code{run_Series()}.

This class is designed to support iterative ABM workflows where users repeatedly
modify settings, generate a \code{Game} object, run simulations, and post-process results.
Each step can be stored as a chunk and executed later in an arbitrary order.
}
\details{
Chunks are typically created using \code{\link[=chunk]{chunk()}}, which captures code blocks
without evaluating them. For example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{chunk(\{
  x <- 1
  y <- x * 2
\})
}\if{html}{\out{</div>}}

The \code{default} slot is intended to represent the initial state of the execution
environment. Users can later override these defaults at runtime via
\code{run_Series(..., input = ...)}.
}
\examples{
S <- ABM_Series(
  chunk_expr = list(
    init = chunk({ x <- 1; y <- 2 }),
    calc = chunk({ z <- add2(x, y) })
  ),
  default = list(
    add2 = function(a, b) a + b
  )
)

str(S)

}
\seealso{
\code{\link[=run_Series]{run_Series()}}, \code{\link[=chunk]{chunk()}}
}
