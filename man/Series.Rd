% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_ABM_Series.R
\name{Series}
\alias{Series}
\alias{as.Series}
\alias{as.Series.ABM_Series}
\alias{as.Series.default}
\alias{print.ABM_Series}
\title{Create an ABM_Series Object}
\usage{
Series(..., default = NULL)

as.Series(x, ...)

\method{as.Series}{ABM_Series}(x, ...)

\method{as.Series}{default}(x, ...)

\method{print}{ABM_Series}(x, max_lines = 6L, ...)
}
\arguments{
\item{...}{Named or unnamed \code{ABM_Chunk} objects created by \code{Chunk()}.
If no name is provided, the variable name is used as the key.
All arguments must be \code{ABM_Chunk} objects; passing other types will
result in an error. Chunk names must be unique.}

\item{default}{An optional list of default objects (values and/or functions)
to be injected into the execution environment before running chunks.
These can be overridden at runtime via \code{run_Series(..., input = ...)}.
If \code{NULL}, an empty default list is stored.}
}
\value{
An object of class \code{"ABM_Series"}, a list with the following elements:
\describe{
\item{chunks}{A named list of \code{ABM_Chunk} objects representing code chunks.}
\item{default}{A named list of default objects (values and/or functions).}
}
}
\description{
\code{Series()} is a constructor for the \code{"ABM_Series"} class.
It stores a set of code chunks (as \code{ABM_Chunk} objects) and default objects
(values and/or functions) that will be used to initialize the execution
environment in \code{run_Series()}.

This class is designed to support iterative ABM workflows where users
define a sequence of steps as \code{ABM_Chunk} objects, which are then
executed later in a controlled environment by \code{run_Series()}.
}
\details{
Chunks are created using \code{Chunk()}, which captures code blocks without
evaluating them. Each chunk is stored as an \code{ABM_Chunk} object and
executed later by \code{run_Series()} in a shared environment, so objects
created in one chunk are available in subsequent chunks.

\if{html}{\out{<div class="sourceCode">}}\preformatted{step1 <- Chunk(\{ x <- 1 \})
step2 <- Chunk(\{ y <- x + 1 \})

S <- Series(step1, step2)
}\if{html}{\out{</div>}}

Named arguments can be used to override the variable name as the key:

\if{html}{\out{<div class="sourceCode">}}\preformatted{S <- Series(init = step1, calc = step2)
}\if{html}{\out{</div>}}

The \code{default} slot represents the initial state of the execution environment.
Users can later override these defaults at runtime via
\code{run_Series(..., input = ...)}.
}
\examples{
step1 <- Chunk({ x <- 1 })
step2 <- Chunk({ y <- x + 1 })

# Basic usage
S <- Series(step1, step2)
str(S)

# With explicit names
S <- Series(init = step1, calc = step2)

# With default objects
S <- Series(
  step1,
  step2,
  default = list(
    add2 = function(a, b) a + b
  )
)

}
\seealso{
\code{\link[=run_Series]{run_Series()}}, \code{\link[=Chunk]{Chunk()}}
}
