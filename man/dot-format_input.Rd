% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_Series.R
\name{.format_input}
\alias{.format_input}
\title{Normalize user input into a named list}
\usage{
.format_input(x, expr, prefix = "Z")
}
\arguments{
\item{x}{An input object to be normalized.}

\item{expr}{The unevaluated expression corresponding to \code{x}
(typically obtained via \code{substitute(x)} at the caller).}

\item{prefix}{A prefix used to generate names when automatic naming is required.}
}
\value{
A named list, or \code{NULL} if \code{x} is \code{NULL}.
}
\description{
Internal utility used by \code{run_Series()} to normalize the \code{input} argument.
}
\details{
\itemize{
\item A single object is wrapped into a list.
\item An unnamed single object may have its name inferred from the call
(via \code{expr}, typically \code{substitute(x)} at the caller).
\item Unnamed multiple inputs are automatically named using \code{prefix} (e.g.
\code{"input1"}, \code{"input2"}, ...).
}

This helper is intentionally kept private to \code{run_Series.R}: it exists to
support \code{run_Series(input = ...)}'s convenience auto-naming, and is not
used elsewhere in the package (in particular, \code{Series(default = ...)}
requires a fully named list explicitly, rather than relying on this
auto-naming behavior -- see \code{Series()}).
}
\keyword{internal}
