% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qap_terms.R
\name{qap_attribute_terms}
\alias{qap_attribute_terms}
\alias{qap_homophily}
\alias{qap_sender}
\alias{qap_receiver}
\alias{qap_attrcross}
\title{QAP terms derived from node attributes}
\usage{
qap_homophily(data, attribute)

qap_sender(data, attribute)

qap_receiver(data, attribute)

qap_attrcross(
  data,
  attribute,
  calc = c("sum", "max", "min", "prod"),
  FUN = NULL,
  ...
)
}
\arguments{
\item{data}{A matrix, typically an adjacency matrix. Its dimensions are used
to shape the output.}

\item{attribute}{A vector of node attributes. Its length must equal
\code{nrow(data)}.}

\item{calc}{A character string specifying how sender and receiver attributes
are combined. One of \code{"sum"}, \code{"max"}, \code{"min"}, or \code{"prod"}.}

\item{FUN}{An optional function used to combine sender and receiver
attributes element-wise. If supplied, \code{calc} is ignored.}

\item{...}{Additional arguments passed to \code{FUN}.}
}
\value{
A matrix with dimensions \code{nrow(data)} by \code{ncol(data)}.
}
\description{
These functions generate matrix-valued QAP terms from a node-level attribute
vector.
}
\details{
\code{qap_homophily()} returns a binary matrix indicating whether sender and
receiver have the same attribute value.

\code{qap_sender()} returns a matrix in which each column contains sender-side
attribute values.

\code{qap_receiver()} returns a matrix in which each row contains receiver-side
attribute values.

\code{qap_attrcross()} combines sender and receiver attributes using either one of
the built-in rules (\code{"sum"}, \code{"max"}, \code{"min"}, \code{"prod"}) or a user-supplied
function.
}
\examples{
mat <- matrix(
  c(0, 1, 1,
    1, 0, 0,
    0, 1, 0),
  nrow = 3, byrow = TRUE
)

attr <- c(1, 2, 1)

qap_homophily(mat, attr)
qap_sender(mat, attr)
qap_receiver(mat, attr)
qap_attrcross(mat, attr, calc = "sum")
qap_attrcross(mat, attr, calc = "max")
qap_attrcross(mat, attr, calc = "prod")
qap_attrcross(mat, attr, FUN = function(x, y) abs(x - y))

}
