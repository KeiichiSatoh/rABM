% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-Game.R
\name{Game}
\alias{Game}
\title{Create an ABM game object}
\usage{
Game(..., time = NULL, log = NULL, notes = NULL)
}
\arguments{
\item{time}{A positive integer time step. If \code{NULL}, the default (\code{1}) is used.}

\item{log}{A list of saved snapshots (default: \code{NULL}).}

\item{notes}{A list of notes (default: \code{NULL}).}

\item{state}{Fields to be stored as \code{"state"}.
Functions are not allowed.}

\item{active_state}{Functions registered as active bindings (\code{"active_state"}).}

\item{act_FUN}{Functions registered as model-level methods (\code{"act_FUN"}).}

\item{stop_FUN}{Functions registered as model-level methods (\code{"stop_FUN"}).}

\item{report_FUN}{Functions registered as model-level methods (\code{"report_FUN"}).}

\item{plot_FUN}{Functions registered as model-level methods (\code{"plot_FUN"}).}
}
\value{
An \code{\link{ABM_Game}} object.
}
\description{
\code{Game()} is the user-facing constructor for an \code{\link{ABM_Game}} object,
the core object of the \strong{rABM} package.
It wraps the internal R6 class \code{\link{ABM_Game}} so that users do not need to interact
with R6 directly.
}
\details{
The \code{ABM_Game} object manages several field categories:
\itemize{
\item \code{"state"}: non-function global fields (e.g., parameters, data objects)
\item \code{"active_state"}: active bindings (functions evaluated on access)
\item \code{"act_FUN"}, \code{"stop_FUN"}, \code{"report_FUN"}, \code{"plot_FUN"}:
functions registered as model-level methods
}

Field names must be unique across all categories.

\code{Game()} internally calls \code{ABM_Game$new()} after normalizing inputs via
\code{.format_input()}.
\subsection{Notes on input}{

When providing inputs for each category, users are encouraged to supply a named list,
because the names are used as field names.
An exception is when a user supplies a single object; in that case, the name may be
inferred from the call and used as the field name.
As a safer option, users can always supply a named list explicitly.
}
}
\examples{
# Basic example
stage1 <- 1
stage2 <- 2
gfun1 <- function() 1
gfun2 <- function() 2

Game(
  stage = list(stage1 = stage1, stage2 = stage2),
  global_FUN = list(gfun1 = gfun1, gfun2 = gfun2)
)

# A single object can be supplied directly
Game(stage = stage1, global_FUN = gfun1)
}
\seealso{
\code{\link{ABM_Game}}, \code{\link[=as.Game]{as.Game()}}
}
