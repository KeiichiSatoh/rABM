% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_ABM_Game.R
\name{Game}
\alias{Game}
\title{Create an ABM game object}
\usage{
Game(..., time = NULL, log = NULL, notes = NULL)
}
\arguments{
\item{...}{\code{\link{ABM_Field}} objects (see \code{\link[=State]{State()}}, \code{\link[=Active]{Active()}},
\code{\link[=Act]{Act()}}, \code{\link[=Stop]{Stop()}}, \code{\link[=Report]{Report()}}, \code{\link[=Plot]{Plot()}}), or \code{\link[=Zip]{Zip()}} bundles
thereof, to be registered on the game.}

\item{time}{A positive integer time step. If \code{NULL}, the default (\code{1}) is used.}

\item{log}{A list of saved snapshots (default: \code{NULL}).}

\item{notes}{A list of notes (default: \code{NULL}).}
}
\value{
An \code{\link{ABM_Game}} object.
}
\description{
\code{Game()} is the user-facing constructor for an \code{\link{ABM_Game}} object,
the core object of the \strong{rABM} package.
It wraps the internal R6 class \code{\link{ABM_Game}} so that users do not need to
interact with R6 directly.
}
\details{
The \code{ABM_Game} object manages several field categories:
\itemize{
\item \code{"state"}: non-function global fields (e.g., parameters, data objects)
\item \code{"active_state"}: active bindings (functions evaluated on access)
\item \code{"act_FUN"}, \code{"stop_FUN"}, \code{"report_FUN"}, \code{"plot_FUN"}:
functions registered as model-level methods
}

Field names must be unique across all categories.

\code{Game()} passes \code{...} directly to \code{ABM_Game$new()}. Each element
of \code{...} must be an \code{\link{ABM_Field}} object -- typically created with
\code{\link[=State]{State()}}, \code{\link[=Active]{Active()}}, \code{\link[=Act]{Act()}}, \code{\link[=Stop]{Stop()}}, \code{\link[=Report]{Report()}}, or
\code{\link[=Plot]{Plot()}} -- or an \code{\link{ABM_Zip}} bundle of such objects created with
\code{\link[=Zip]{Zip()}}, which is flattened automatically.
}
\examples{
pop <- 100
growth_rate <- function(rate = 1.05) { self$pop * rate }
reproduce   <- function() { self$pop <- self$pop * 1.1 }

G <- Game(
  State(pop),
  Active(growth_rate),
  Act(reproduce)
)
G

# ABM_Field objects can also be bundled with Zip() and passed as one argument
common_fields <- Zip(State(pop), Active(growth_rate))
G2 <- Game(common_fields, Act(reproduce))
}
\seealso{
\code{\link{ABM_Game}}, \code{\link{Field}}, \code{\link{Zip}}
}
