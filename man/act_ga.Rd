% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/act_ga.R
\name{act_ga}
\alias{act_ga}
\title{Genetic algorithm update}
\usage{
act_ga(
  parents,
  fit_score,
  ga_type = c("binary", "integer", "numeric"),
  n_elite = 2,
  prob_crossover = 0.8,
  prob_mutation = 0.1,
  values_min = NULL,
  values_max = NULL,
  type_selection_prob = c("fit_prop"),
  type_crossover = NULL,
  type_mutation = NULL,
  parent_info = FALSE,
  ...
)
}
\arguments{
\item{parents}{A matrix or data.frame in which each row represents one
parent individual and each column represents one DNA element.}

\item{fit_score}{A numeric vector of fitness scores for each parent.
Larger values indicate better fitness.}

\item{ga_type}{A character string specifying the genetic representation.
Must be one of \code{"binary"}, \code{"integer"}, or \code{"numeric"}.}

\item{n_elite}{A single non-negative integer giving the number of elite
parents preserved unchanged in the next generation.}

\item{prob_crossover}{A single numeric value between 0 and 1 giving the
probability of crossover for each child.}

\item{prob_mutation}{A single numeric value between 0 and 1 giving the
mutation probability for each DNA element.}

\item{values_min}{Optional lower bounds used during mutation for
\code{"integer"} and \code{"numeric"} types. Must be \code{NULL}, length 1, or
length \code{ncol(parents)}. Ignored when \code{ga_type = "binary"}.}

\item{values_max}{Optional upper bounds used during mutation for
\code{"integer"} and \code{"numeric"} types. Must be \code{NULL}, length 1, or
length \code{ncol(parents)}. Ignored when \code{ga_type = "binary"}.}

\item{type_selection_prob}{Method used to convert fitness scores into
parent-selection probabilities. Currently only \code{"fit_prop"} is supported.}

\item{type_crossover}{Type of crossover. Allowed values depend on \code{ga_type}:
\describe{
\item{\code{"binary"}}{\code{"one"}}
\item{\code{"integer"}}{\code{"one"}}
\item{\code{"numeric"}}{\code{"one"} or \code{"prop"}}
}
If \code{NULL}, a default is chosen automatically.}

\item{type_mutation}{Type of mutation. Allowed values depend on \code{ga_type}:
\describe{
\item{\code{"binary"}}{ignored}
\item{\code{"integer"}}{\code{"uniform"} or \code{"walk"}}
\item{\code{"numeric"}}{\code{"walk"} or \code{"uniform"}}
}
If \code{NULL}, a default is chosen automatically for non-binary types.}

\item{parent_info}{Logical. If \code{TRUE}, return a list containing both the
children matrix and a two-column matrix indicating the parent indices used
to generate each row.}

\item{...}{Reserved for future extensions.}
}
\value{
If \code{parent_info = FALSE}, a matrix representing the next generation.

If \code{parent_info = TRUE}, a list with the following elements:
\describe{
\item{children}{A matrix representing the next generation.}
\item{parent_info}{A two-column integer matrix with columns \code{parent1}
and \code{parent2}, indicating the parent indices used to generate each row
of \code{children}. For elite rows, the same parent index is repeated twice.}
}
}
\description{
Perform one generation update of a genetic algorithm population.
}
\details{
Each row of \code{parents} represents one individual, and each column
represents one DNA element. The function supports binary, integer,
and numeric search spaces through \code{ga_type}.

The update consists of:
\enumerate{
\item elite preservation,
\item fitness-based parent selection,
\item crossover,
\item mutation.
}

\code{act_ga()} performs a single-generation GA update rather than a full
optimization run. It is intended for situations where users want to
control the update cycle explicitly.

The interpretation of crossover and mutation depends on \code{ga_type}:
\describe{
\item{Binary}{One-point crossover and bit-flip mutation.}
\item{Integer}{One-point crossover and either integer uniform-replacement
mutation or integer random-walk mutation.}
\item{Numeric}{One-point or proportional crossover, and either numeric
random-walk mutation or numeric uniform-replacement mutation.}
}

When \code{ga_type = "binary"}, \code{values_min}, \code{values_max}, and
\code{type_mutation} are ignored.
}
\examples{
set.seed(1)

#--------------------------------
# binary example
#--------------------------------
parents_bin <- matrix(sample(0:1, size = 20, replace = TRUE), nrow = 5)
fit_bin <- c(3.2, 1.5, 4.8, 2.0, 3.9)

act_ga(
  parents = parents_bin,
  fit_score = fit_bin,
  ga_type = "binary"
)

#--------------------------------
# integer example
#--------------------------------
parents_int <- matrix(sample(1:5, size = 24, replace = TRUE), nrow = 6)
fit_int <- c(2.1, 5.4, 1.3, 4.0, 3.7, 2.8)

act_ga(
  parents = parents_int,
  fit_score = fit_int,
  ga_type = "integer",
  type_mutation = "walk",
  values_min = 1,
  values_max = 5
)

#--------------------------------
# numeric example
#--------------------------------
parents_num <- matrix(stats::runif(30), nrow = 6)
fit_num <- c(1.1, 2.3, 4.8, 3.5, 2.7, 5.0)

act_ga(
  parents = parents_num,
  fit_score = fit_num,
  ga_type = "numeric",
  type_crossover = "prop",
  type_mutation = "walk",
  values_min = 0,
  values_max = 1
)

#--------------------------------
# return parent information
#--------------------------------
res <- act_ga(
  parents = parents_bin,
  fit_score = fit_bin,
  ga_type = "binary",
  parent_info = TRUE
)

str(res)

}
\seealso{
\code{\link[GA]{ga}} in the \pkg{GA} package for a full genetic algorithm
implementation.
}
