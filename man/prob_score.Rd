% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prob_score.R
\name{prob_score}
\alias{prob_score}
\alias{prob_linprop}
\alias{prob_power}
\alias{prob_softmax}
\alias{prob_rank}
\alias{prob_eps_greedy}
\title{Score-to-probability transformation rules}
\usage{
prob_linprop(x)

prob_power(x, alpha = 1)

prob_softmax(x, beta = 1)

prob_rank(
  x,
  ties.method = c("max", "average", "first", "last", "random", "min")
)

prob_eps_greedy(x, eps = 0.1)
}
\arguments{
\item{x}{A numeric vector or numeric matrix of scores.

If \code{x} is a matrix, each row is interpreted as a separate decision context.}

\item{alpha}{A non-negative exponent used in \code{\link[=prob_power]{prob_power()}}. Larger values
place more probability mass on higher scores. \code{alpha = 1} corresponds to
linear proportional transformation.}

\item{beta}{A non-negative sensitivity parameter used in \code{\link[=prob_softmax]{prob_softmax()}}.
Larger values make the distribution more concentrated on higher scores.}

\item{ties.method}{A character string specifying how ties are handled in
\code{\link[=prob_rank]{prob_rank()}}. Passed to \code{\link[=rank]{rank()}} or \code{matrixStats::rowRanks()}.}

\item{eps}{An exploration parameter used in \code{\link[=prob_eps_greedy]{prob_eps_greedy()}}. With
probability \code{1 - eps}, the best option is chosen; with probability \code{eps},
exploration is introduced uniformly across options.}
}
\value{
A numeric vector or numeric matrix of probabilities with the same shape as
\code{x}.

If \code{x} is a matrix, each row sums to one unless all entries in that row are
unavailable.
}
\description{
Convert a numeric score vector or score matrix into a probability distribution.
}
\details{
These functions implement several common probability transformation rules used
in agent-based modeling, reinforcement learning, and evolutionary computation.
Each function maps scores to probabilities that sum to one.

If \code{x} is a matrix, each row is treated as an independent choice set and
probabilities are normalized row-wise.

Missing values (\code{NA}) are treated as unavailable choices. In the returned
probabilities, such entries are assigned zero.

The implemented rules are as follows.

\strong{Linear proportional transformation}

Scores are shifted if necessary so that all values become non-negative, and
then normalized:

\deqn{p_i = x_i / \sum_j x_j}

In \code{\link[=prob_linprop]{prob_linprop()}}, if any score is negative, the global minimum of \code{x} is
subtracted from all scores before normalization.

\strong{Power transformation}

Scores are shifted to non-negative values if necessary, raised to the power
\code{alpha}, and normalized:

\deqn{p_i = x_i^\alpha / \sum_j x_j^\alpha}

In \code{\link[=prob_power]{prob_power()}}, \code{alpha = 1} gives the same result as linear proportional
transformation. Larger values increase selection pressure; values between
\code{0} and \code{1} flatten the distribution.

\strong{Softmax transformation}

Scores are exponentiated after scaling by \code{beta}:

\deqn{p_i = \exp(\beta x_i) / \sum_j \exp(\beta x_j)}

\code{\link[=prob_softmax]{prob_softmax()}} corresponds to the standard softmax (or Boltzmann / logit)
rule. Larger \code{beta} gives stronger concentration on high-score options.

\strong{Rank-based transformation}

Scores are converted to ranks and normalized:

\deqn{p_i = r_i / \sum_j r_j}

where \eqn{r_i} denotes the rank of score \eqn{x_i}.

\strong{Epsilon-greedy transformation}

The highest-score option receives probability

\deqn{1 - \varepsilon + \varepsilon / n}

while all options receive a uniform exploration mass

\deqn{\varepsilon / n}

before unavailable (\code{NA}) choices are set to zero and the result is
renormalized if needed.
}
\section{Included functions}{

\describe{
\item{\code{\link[=prob_linprop]{prob_linprop()}}}{Linear proportional transformation.}
\item{\code{\link[=prob_power]{prob_power()}}}{Power transformation.}
\item{\code{\link[=prob_softmax]{prob_softmax()}}}{Softmax transformation.}
\item{\code{\link[=prob_rank]{prob_rank()}}}{Rank-based transformation.}
\item{\code{\link[=prob_eps_greedy]{prob_eps_greedy()}}}{Epsilon-greedy transformation.}
}
}

\examples{
x <- c(1, 2, 3, 4)

prob_linprop(x)
prob_power(x, alpha = 2)
prob_softmax(x, beta = 1)
prob_rank(x)
prob_eps_greedy(x, eps = 0.1)

m <- matrix(c(
  1, 2, 3, 4,
  4, 3, 2, 1
), nrow = 2, byrow = TRUE)

prob_linprop(m)
prob_power(m, alpha = 2)
prob_softmax(m, beta = 1)
prob_rank(m)
prob_eps_greedy(m, eps = 0.1)

m2 <- matrix(c(
  1, NA, 3, 4,
  NA, 3, 2, 1
), nrow = 2, byrow = TRUE)

prob_softmax(m2, beta = 1)
prob_rank(m2)
prob_eps_greedy(m2, eps = 0.1)

}
