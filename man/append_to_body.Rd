% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_stop_convergence.R,
%   R/build_stopFUN_convergence.R
\name{append_to_body}
\alias{append_to_body}
\title{Append a new code block to the body of a function}
\usage{
append_to_body(FUN, expr, posit = c("after", "before"))

append_to_body(FUN, expr, posit = c("after", "before"))
}
\arguments{
\item{FUN}{The original function.}

\item{expr}{A new code block created by \code{quote({...})} or
\code{bquote({...})}.}

\item{posit}{Whether to insert \code{expr} \code{"before"} or
\code{"after"} the existing body. Defaults to \code{"after"}.}
}
\value{
A new function with the combined body. \code{FUN} itself is not
modified in place.

A new function with the combined body. \code{FUN} itself is not
modified in place.
}
\description{
Internal helper used by \code{\link{add_stop_convergence}} (and
potentially other \code{add_*} generators) to splice an additional
\code{{...}} block of statements into an existing function's body,
either before or after its current statements.

Internal helper used by \code{\link{build_stopFUN_convergence}} (and
potentially other \code{build_*} generators) to splice an additional
\code{{...}} block of statements into an existing function's body,
either before or after its current statements.
}
\seealso{
\code{\link{add_stop_convergence}}

\code{\link{build_stopFUN_convergence}}
}
\keyword{internal}
